// src/sections/PodcastSection.jsx
// The homepage "About" section. Renders the shared AboutContent so the
// standalone /about/ page and this section stay in sync.
import React from 'react';
import AboutContent from '../components/AboutContent';

const PodcastSection = () => <AboutContent />;

export default PodcastSection;
