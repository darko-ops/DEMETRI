// src/sections/SoundSection.jsx
// The homepage "Sound" section. Renders the shared SoundContent so the
// standalone /sound/ page and this section stay in sync.
import React from 'react';
import SoundContent from '../components/SoundContent';

const SoundSection = () => <SoundContent />;

export default SoundSection;
